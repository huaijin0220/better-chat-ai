# 变更记录

本项目的重要变更记录在此文件中。

## [1.3.0] - 2026-07-25

### 新增

- 多平台适配：Cursor（`.cursor/rules/*.mdc`）、GitHub Copilot（`.github/copilot-instructions.md`）、Windsurf（`.windsurf/rules/*.md`）
- 各平台专用分支：`cursor`、`copilot`、`windsurf`

## [1.2.0] - 2026-07-25

### 新增

- 英文版 `en` 分支，文件名为 `SKILL.md`，AI 可直接识别，无需特殊命名

### 变更

- 审查结果汇报改为结构化模板，统一输出格式

## [1.1.0] - 2026-07-20

### 新增

- 项目审查功能：双重验证机制（阶段一生成前意图确认 + 阶段二生成后产物审查）
- Codex CLI 专用分支（`codex`），frontmatter 仅保留 `name` 和 `description`
- README 新增项目目的章节
- 功能列表按流程阶段分类整理（需求澄清、执行授权、安全隐私、验证审查、范围控制）

### 变更

- 合并"验证与汇报"到"项目审查"，消除内容冗余
- frontmatter 改为 `mode: always` 持久行为层，移除条件触发
- 目录结构调整：`usage.md` 移至 `examples/` 目录

### 移除

- 删除 `claude-cli` 分支（与 main 一致，无存在必要）

## [1.0.0] - 2026-07-20

### 新增

- 首次发布 `better-chat-ai` 技能包。
- 增加知识问答与操作型任务的分类规则。
- 增加需求澄清、方案复述和执行授权流程。
- 增加普通操作直接执行与高风险操作额外确认机制。
- 增加最小范围访问、敏感信息保护和可回退要求。
- 增加执行后验证、失败披露和结果汇报规范。
- 增加安装、使用、行为边界和典型场景示例。
